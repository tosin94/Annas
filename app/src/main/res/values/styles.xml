<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="gallery_appBar" parent="Base.Theme.AppCompat.Light.DarkActionBar"/>

    <style name="GalleryTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorBackground">@android:color/black</item>
        <item name="colorPrimaryDark">@color/statusBarGallery</item>
        <item name="colorPrimary">@color/statusBarGallery</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/context_style</item>
    </style>
    
    <style name="context_style" parent="@style/Widget.AppCompat.ActionMode"><!--
    for the context menu-->
        <item name="background">@color/statusBarGallery</item>
    </style>

</resources>
